---
phase: 01-alina-caption-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/alina_textgen.py
autonomous: true
requirements:
  - R1
must_haves:
  truths:
    - CLI prints N captions with text, platform, hook_type, cta, hashtags
    - Same seed produces identical output
  artifacts:
    - scripts/alina_textgen.py
  key_links: []
---

<objective>
Build the standalone identity-locked caption generator for Alina Sky.

Purpose: Operator generates platform-optimized on-screen captions for Alina's alt-girl content (IG/TikTok/X/YouTube/FB) without manual text writing. This is Phase 1 of the caption generator milestone; Phases 2-4 wire it into the server and web UI.

Output: `scripts/alina_textgen.py` — a zero-dependency, pool-based caption generator (no AI API) with 2x pools, 5 hook types grounded in Alina's identity, per-platform CTA + hashtag configs, seed reproducibility, and a CLI.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant existing code:
@scripts/run_tiktok.py  (CLI pattern with argparse, _load_active_key, day_path usage)
@core/text_generator.py (existing pool-based generator to model structure after, but this is a NEW standalone module)
@docs/alina_style_guide.md (Alina identity reference)
@docs/wavespeed_identity_alina.md (Alina Sky identity: name, avatar)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OPENERS/MIDDLES/CLOSERS pools (2x size, 5 hook types)</name>
  <files>scripts/alina_textgen.py</files>
  <read_first>docs/alina_style_guide.md, docs/wavespeed_identity_alina.md</read_first>
  <action>Create `scripts/alina_textgen.py`. Define OPENERS dict with keys: vulnerable, confident, playful, aesthetic, relatable — 20 items each. MIDDLES dict: same 5 keys, 12 items each. CLOSERS dict: same 5 keys, 12 items each. Content must be Alina-identity grounded: alt-girl baddie, raw amateur iPhone aesthetic, damp/wet hair, black band tees, 3am texts, mirror checks, fit checks, night drives, dark rooms, moody candid energy. Match tone in docs/alina_style_guide.md. All strings short enough for 6s on-screen captions (~40-90 chars per segment).</action>
  <verify>python -c "import sys; sys.path.insert(0,'scripts'); import alina_textgen as a; print({k:len(v) for k,v in a.OPENERS.items()}); print({k:len(v) for k,v in a.MIDDLES.items()}); print({k:len(v) for k,v in a.CLOSERS.items()})"</verify>
  <acceptance_criteria>
    - scripts/alina_textgen.py contains "OPENERS" dict with 5 keys each having 20 items
    - MIDDLES and CLOSERS each have 5 keys with 12 items
    - File imports cleanly with no third-party deps
  </acceptance_criteria>
  <done>OPENERS=5x20, MIDDLES=5x12, CLOSERS=5x12; file imports cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add PLATFORM_CONFIG with per-platform cta_pool + hashtags</name>
  <files>scripts/alina_textgen.py</files>
  <action>Define PLATFORM_CONFIG dict with keys: tiktok, reels, shorts, x, stories. Each value = dict with `cta_pool` (list of 4+ call-to-action strings) and `hashtags` (list of strings). Required hashtag counts: tiktok=8 (#altgirl #altingirl #darkaesthetic #relatable #softgirl #mentalhealth #altgirlproblems #fyp), reels=9, shorts=8, x=3, stories=0 (empty list, hashtags omitted in output). CTAs match platform voice (e.g. TikTok/Reels: "follow for part two", "which fit should I wear" — engagement hooks; X: thread-style invites).</action>
  <verify>python -c "import sys; sys.path.insert(0,'scripts'); import alina_textgen as a; print({k:(len(v['hashtags'])) for k,v in a.PLATFORM_CONFIG.items()})"</verify>
  <acceptance_criteria>
    - PLATFORM_CONFIG has all 5 platform keys
    - Each has cta_pool (>=4) and hashtags (counts 8/9/8/3/0)
    - tiktok hashtags match the 8 required strings exactly
  </acceptance_criteria>
  <done>PLATFORM_CONFIG complete; hashtag counts 8/9/8/3/0 verified</done>
</task>

<task type="auto">
  <name>Task 3: Implement generate_caption + batch_generate with seed support and dedup</name>
  <files>scripts/alina_textgen.py</files>
  <action>Implement `generate_caption(hook_type=None, platform="tiktok", seed=None) -> dict`. When seed given: `random.seed(seed)` before selection (full reproducibility). hook_type None or unknown key → random.choice of the 5 keys. Caption structure: opener + 0-2 middles + closer, joined ". ", final "." appended. Return `{"text": ..., "platform": ..., "hook_type": ..., "cta": ..., "hashtags": [...]}`. cta = random.choice(platform cta_pool). Implement `batch_generate(count, platforms=None, hook_types=None, seed=None) -> list[dict]`: count captions, dedup by text (regenerate on collision, guard against infinite loop with a cap of 200 attempts), platforms list selects platform per caption (default all 5 or ["tiktok"] if given a single string), hook_types list filters hook selection. Use random.Random instance optionally but seed must be honored. Do not mutate pools. All stdlib only (import random).</action>
  <verify>python -c "import sys; sys.path.insert(0,'scripts'); import alina_textgen as a; x=a.batch_generate(5, platforms=['tiktok'], seed=7); print(len(x)); print(len({c['text'] for c in x})); print(a.batch_generate(2, platforms=['tiktok'], seed=7)[0]['text']==a.batch_generate(2, platforms=['tiktok'], seed=7)[0]['text'])"</verify>
  <acceptance_criteria>
    - generate_caption returns dict with exactly text/platform/hook_type/cta/hashtags keys
    - batch_generate(count) returns count unique texts
    - Same seed + same args → identical output
  </acceptance_criteria>
  <done>batch returns N unique dicts; seed reproducibility verified</done>
</task>

<task type="auto">
  <name>Task 4: Add CLI main() with argparse + stdout printing</name>
  <files>scripts/alina_textgen.py</files>
  <action>Add `main()` guarded by `if __name__ == "__main__":`. argparse: positional `n` (type=int, default 10), optional positional `platform` (nargs='?', choices=list(PLATFORM_CONFIG), default "tiktok"), `--seed` (type=int, default None). For each caption print: `--- {i} [{platform}] [{hook_type}] ---`, then text, then `#` + hashtags joined by space on next line (skip hashtags line when empty). Exit 0. File must be directly runnable: `python scripts/alina_textgen.py 10`.</action>
  <verify>python scripts/alina_textgen.py 5 --seed 1 2>&1 | head -20; python -m py_compile scripts/alina_textgen.py</verify>
  <acceptance_criteria>
    - scripts/alina_textgen.py has `if __name__ == "__main__":` guard
    - CLI prints 5 captions with --- i [PLATFORM] [HOOK] --- header, text, hashtags
    - python -m py_compile scripts/alina_textgen.py exits 0
  </acceptance_criteria>
  <done>CLI runs standalone; py_compile clean</done>
</task>

</tasks>
