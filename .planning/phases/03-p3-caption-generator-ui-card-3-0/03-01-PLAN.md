---
phase: 03-caption-generator-ui
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - webui/static/index.html
  - webui/static/app.js
  - webui/static/style.css
autonomous: true
requirements:
  - R1
must_haves:
  truths:
    - Caption Generator card renders in the page with platform pills, hook-type pills, count slider, and Generate button
    - Generating posts to /api/captions/generate and renders returned captions with per-caption copy, copy-all, and clear
    - Copy/copy-all writes to clipboard and shows a toast
  artifacts:
    - webui/static/index.html (caption-gen card markup)
    - webui/static/app.js (getSelectedCapPlatform, getSelectedCapHook, generateCaptions, renderCaptions, copyAllCaptions, clearCaptions)
    - webui/static/style.css (.caption-gen-card, .caption-item, .cap-badge, .cap-actions styles)
  key_links:
    - webui/server.py:664 (POST /api/captions/generate endpoint consumed by generateCaptions)
---

<objective>
Add a Caption Generator card to the OFM web UI. Operator picks a platform (tiktok/reels/shorts/x/stories), a hook type (vulnerable/confident/playful/aesthetic/relatable/mixed), a count (1-20), clicks Generate, and gets platform-optimized Alina captions with one-click copy. This is Phase 3 of the caption generator milestone; the Phase 2 endpoint `/api/captions/generate` already exists and is verified live on :8000.

Output: new card in the SPA (index.html + app.js + style.css) consuming the Phase 2 API contract.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

API contract (verified Phase 2): POST /api/captions/generate body {count(1-20), platform(tiktok|reels|shorts|x|stories), hook_types?[list], seed?int} → {ok:true, captions:[{text, platform, hook_type, cta, hashtags:[...]}]}. hook_types omitted for "mixed".

Existing UI structure (verified):
- index.html: `.gen-layout` div (lines 262-344) holds left `.card` (Image Generation) + right `.gen-preview`. `.out-header` "Outputs" begins line 346. New caption card inserts between line 344 (`</div><!-- /gen-layout -->`) and line 346.
- Existing pill pattern (index.html:270-313): `<div class="field"><label>X</label><div class="vibe-pills"><label class="pill"><input type="radio" name="..." value="..." checked onchange="..."> Label</label>...`.
- Count slider pattern (index.html:317): `<input type="range" id="photo-count" min="1" max="30" class="photo-slider" oninput="updateCost(); document.getElementById('photo-count-label').textContent=this.value">`.
- Generate button pattern (index.html:320): `<button class="btn" id="btn-photo" style="width:100%"><span class="btn-text">Generate</span></button>`.
- app.js helpers: `api(url, body)` (line 773) async → JSON; `showToast(message, type, duration)` (line 958); `esc(s)` HTML-escape (line 1215); getSelected* radio pattern (lines 793-830). DOMContentLoaded init block line 2224. Equal-height sync at 2260 (card = .gen-layout .card — syncPanelHeights must not break with new card).
- style.css: `.card` (501, max-width 360px, border-top 2px accent), `.card h3::before` content '> ' accent (523), `.field` (640/2306), `.vibe-pills` (652), `.pill:has(input:checked)` accent bg (678), `.photo-slider` (687), `.btn` (728), `.btn-sm` (755), `.btn-outline` (756), `.prompt-item` (2059) list-item pattern to mirror, responsive @media max-width:768px at 631.
- IMPORTANT: user previously requested `#btn-photo .btn-text` style button-label pattern (`_btnTxt` at app.js:911). New caption button should follow same pattern. Pills use `pill:has(input:checked)` CSS so NO JS pill-highlighting needed.
- IMPORTANT: `setControlsLocked` targets `.gen-layout .card.controls-locked` — new caption card is OUTSIDE .gen-layout so unaffected. Must NOT add id conflicts (#btn-photo is separate; use #btn-caption).
- Security note from Phase 1 review: HTML-escape caption text before DOM render (use existing `esc()`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Caption Generator card HTML</name>
  <files>webui/static/index.html</files>
  <read_first>webui/static/index.html</read_first>
  <action>Insert between `</div><!-- /gen-layout -->` (line 344) and `.out-header` (line 346) a new `<div class="card caption-gen-card">` with:
  - `<h3>Caption Generator</h3>`
  - Platform field: `.vibe-pills` radios name="cap_platform", values tiktok (checked)/reels/shorts/x/stories, labels "TikTok"/"Reels"/"Shorts"/"X"/"Stories".
  - Hook field: radios name="cap_hook", values vulnerable (checked)/confident/playful/aesthetic/relatable/mixed, labels matching.
  - Count field: label "Count: <span id="cap-count-label">5</span>", `<input type="range" id="cap-count" min="1" max="20" value="5" class="photo-slider" oninput="document.getElementById('cap-count-label').textContent=this.value">`.
  - `<button class="btn" id="btn-caption" style="width:100%"><span class="btn-text">Generate Captions</span></button>`.
  - `<div id="caption-list" class="caption-list"></div>`.
  - Actions row `<div class="cap-actions">`: `<button class="btn btn-sm btn-outline" onclick="copyAllCaptions()" id="btn-copy-all" style="display:none;">Copy All</button>` + `<button class="btn btn-sm btn-outline" onclick="clearCaptions()" id="btn-clear-caps" style="display:none;">Clear</button>`.
  All labels via `.pill` + hidden radio inputs exactly like existing fields.</action>
  <verify>grep 'caption-gen-card' webui/static/index.html → present; grep 'id="btn-caption"' → present; grep 'name="cap_platform"' → 5 inputs; grep 'name="cap_hook"' → 6 inputs</verify>
  <acceptance_criteria>
    - New card sits between gen-layout and Outputs header
    - 5 platform pills, 6 hook pills, count slider, Generate button, empty caption list container, hidden Copy All + Clear buttons
  </acceptance_criteria>
  <done>Card markup present with all controls</done>
</task>

<task type="auto">
  <name>Task 2: JS generator handlers</name>
  <files>webui/static/app.js</files>
  <read_first>webui/static/app.js</read_first>
  <action>Add near the Photo Generation Controls section (after line 830):
  - `function getSelectedCapPlatform()` — radio name="cap_platform", default "tiktok".
  - `function getSelectedCapHook()` — radio name="cap_hook", default "vulnerable".
  - `async function generateCaptions()` — read count from #cap-count (int), platform, hook. Build body: {count, platform}; if hook !== "mixed" → body.hook_types=[hook]. POST via `api('/api/captions/generate', body)`. On ok: call renderCaptions(caps), reveal #btn-copy-all + #btn-clear-caps, showSuccess toast. On !ok: showError. Disable #btn-caption while fetching (btn.disabled=true, `#btn-caption .btn-text` textContent 'Generating...'), restore after.
  - `function renderCaptions(caps)` — for each cap build a `.caption-item` div: head row with platform + hook_type badges (`.cap-badge`), body `<pre class="caption-text-raw">` with `esc(cap.text)`, meta line with cta + hashtags (esc'd, hashtags joined with space), copy button per item (`onclick="copyCaption(N)"` or inline data-index). Store caps in module var `_captions = caps` for copy-by-index. Use only `esc()` (NO innerHTML of raw strings).
  - `function copyCaption(idx)` — navigator.clipboard.writeText(_captions[idx].text) then showSuccess toast 'Caption copied'.
  - `async function copyAllCaptions()` — join all _captions texts with "\n\n", clipboard, showSuccess 'N captions copied'.
  - `function clearCaptions()` — empty #caption-list, hide Copy All + Clear, reset _captions, showToast 'Cleared'.
  All wire via global onclick (inline `onclick="generateCaptions()"` on #btn-caption) consistent with existing `onclick="startPromptGeneration()"` style.</action>
  <verify>node --check webui/static/app.js; grep 'function generateCaptions' app.js; grep 'function renderCaptions' app.js; grep 'function copyAllCaptions' app.js; grep 'function clearCaptions' app.js; grep 'function copyCaption' app.js</verify>
  <acceptance_criteria>
    - All 6 functions defined
    - node --check clean
    - Uses api()/showToast/esc() helpers, no XSS (all dynamic text esc()'d)
  </acceptance_criteria>
  <done>Handlers present, syntax clean, esc() used on all dynamic text</done>
</task>

<task type="auto">
  <name>Task 3: CSS for caption card</name>
  <files>webui/static/style.css</files>
  <read_first>webui/static/style.css</read_first>
  <action>Append at end of style.css (after line 2789) a Caption Generator section:
  - `.caption-gen-card { max-width: 100%; }` (full width below gen-layout).
  - `.caption-list { margin-top: 16px; display: flex; flex-direction: column; gap: 10px; max-height: 420px; overflow-y: auto; }`.
  - `.caption-item { border: 1px solid var(--border); border-left: 2px solid var(--accent); border-radius: var(--radius); padding: 12px; background: var(--surface); }`.
  - `.caption-item-head { display: flex; gap: 8px; margin-bottom: 8px; }`.
  - `.cap-badge { font-family: var(--font-mono); font-size: 10px; text-transform: uppercase; letter-spacing: 0.5px; padding: 2px 8px; border: 1px solid var(--border); border-radius: 9999px; color: var(--accent); }`.
  - `.caption-text-raw { white-space: pre-wrap; word-wrap: break-word; font-family: var(--font-mono); font-size: 12px; color: var(--fg); margin: 0 0 8px; }`.
  - `.caption-meta { font-size: 11px; color: var(--fg2); margin-bottom: 8px; }`.
  - `.cap-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 12px; }`.
  - `.caption-empty { padding: 20px; text-align: center; color: var(--fg2); border: 1px dashed var(--border); border-radius: var(--radius); }`.
  - Responsive: `@media (max-width: 768px) { .cap-actions { flex-wrap: wrap; } .caption-item { padding: 10px; } }`.
  All colors via theme vars only (--surface, --border, --accent, --fg, --fg2, --radius).</action>
  <verify>node --check not needed (CSS); read tail of style.css → section present; server serves /static/style.css 200</verify>
  <acceptance_criteria>
    - Classes defined with theme vars
    - Responsive block present
  </acceptance_criteria>
  <done>CSS classes present, theme-var based, responsive</done>
</task>

<task type="auto">
  <name>Task 4: Live integration test</name>
  <files></files>
  <read_first></read_first>
  <action>Test against running server (Phase 2 executor left server PID 16492 on :8000 — restart if dead via `python webui/server.py` background). Verify:
  1. `python -m py_compile webui/static/index.html` is NOT valid (html) — instead check `node --check webui/static/app.js` clean.
  2. curl POST /api/captions/generate {"count":3,"platform":"tiktok","hook_types":["vulnerable"],"seed":1} → ok true, 3 captions.
  3. GET / → 200, served index.html contains 'caption-gen-card' and 'btn-caption'.
  4. GET /static/app.js → 200 contains 'generateCaptions'.
  5. GET /static/style.css → 200 contains '.caption-gen-card'.
  6. Smoke: JS has no reference to undefined helper (grep each new function's called name).
  Record results in SUMMARY.md.</action>
  <verify>py_compile webui/server.py clean (no server change needed); node --check webui/static/app.js clean; all 5 HTTP checks pass</verify>
  <acceptance_criteria>
    - Server serves updated index.html/app.js/style.css with caption card
    - Endpoint still returns valid captions
  </acceptance_criteria>
  <done>Live integration verified, SUMMARY.md written</done>
</task>

</tasks>
