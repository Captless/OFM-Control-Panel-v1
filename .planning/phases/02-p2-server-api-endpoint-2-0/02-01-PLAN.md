---
phase: 02-server-api-endpoint
plan: 01
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - webui/server.py
autonomous: true
requirements:
  - R1
must_haves:
  truths:
    - POST /api/captions/generate returns JSON with ok:true and captions list
    - Each caption dict has text, platform, hook_type, cta, hashtags keys
    - Invalid count type returns 400
  artifacts:
    - webui/server.py imports batch_generate from alina_textgen
  key_links:
    - webui/server.py endpoint -> scripts/alina_textgen.py batch_generate
---

<objective>
Wire the Phase 1 caption generator into the web server.

Purpose: Expose `POST /api/captions/generate` so the web UI (Phase 3) can request captions. This is Phase 2 of the caption generator milestone; Phase 3 builds the UI card on top of this endpoint.

Output: `webui/server.py` gains an import of `batch_generate` from `scripts/alina_textgen.py` and a new POST handler `POST /api/captions/generate`.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/User/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-p1-alina-text-generator-module-1-0/01-01-SUMMARY.md

Relevant source files:
@webui/server.py  (do_POST at line 621; body read via self._read_body() at line 635; self._json(data, status) helper at line 890; sys.path.insert pattern at top lines 24-32)
@scripts/alina_textgen.py  (batch_generate(count, platforms=None, hook_types=None, seed=None) -> list[dict]; raises ValueError on dedup cap exhaustion or empty platforms)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import batch_generate from scripts</name>
  <files>webui/server.py</files>
  <read_first>webui/server.py</read_first>
  <action>In `webui/server.py`, after the existing `API_DIR = BASE / "api"` block (around line 29-31), add: `SCRIPTS_DIR = BASE / "scripts"`, then `sys.path.insert(0, str(SCRIPTS_DIR))` and `from alina_textgen import batch_generate`. Must come after BASE is defined (line 19). Do NOT move existing sys.path inserts. Note: `alina_textgen` uses only stdlib (random, argparse) — no import side effects, safe to import in server.</action>
  <verify>python -c "import sys; sys.path.insert(0,'.'); sys.path.insert(0,'webui'); import server" 2>&1 | Select-Object -First 5</verify>
  <acceptance_criteria>
    - webui/server.py contains "SCRIPTS_DIR = BASE / \"scripts\""
    - webui/server.py contains "from alina_textgen import batch_generate"
    - Module imports without error
  </acceptance_criteria>
  <done>Import block added; server module imports cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /api/captions/generate handler</name>
  <files>webui/server.py</files>
  <read_first>webui/server.py</read_first>
  <action>In `do_POST`, after `body = self._read_body()` (line 635), add a branch BEFORE the identity handler: `if parsed.path == "/api/captions/generate":`. Parse: `count = body.get("count", 10)` → validate int (reject non-int with `self._json({"ok": False, "error": "count must be an integer"}, 400)`), clamp to 1-20 (`max(1, min(20, count))`). `platform = (body.get("platform") or "tiktok").strip()` — if platform not in alina_textgen.PLATFORM_CONFIG, return 400 `{"ok": False, "error": "unknown platform"}`. `hook_types = body.get("hook_types")` (list, optional — validate list or None, else 400). `seed = body.get("seed")` (int or None — reject non-int with 400). Call `caps = batch_generate(count, platforms=[platform], hook_types=hook_types, seed=seed)`. Respond `self._json({"ok": True, "captions": caps})`. Wrap the batch_generate call in try/except ValueError → `self._json({"ok": False, "error": str(e)}, 400)` and except Exception → `self._json({"ok": False, "error": str(e)}, 500)`. Return after the branch. Import `PLATFORM_CONFIG` too: `from alina_textgen import batch_generate, PLATFORM_CONFIG`.</action>
  <verify>python -m py_compile webui/server.py</verify>
  <acceptance_criteria>
    - webui/server.py contains '"/api/captions/generate"'
    - Handler reads count/platform/hook_types/seed with validation
    - Non-int count returns 400, unknown platform returns 400
    - py_compile exits 0
  </acceptance_criteria>
  <done>Endpoint branch added; py_compile clean</done>
</task>

<task type="auto">
  <name>Task 3: Live-test endpoint</name>
  <files>webui/server.py</files>
  <read_first>webui/server.py</read_first>
  <action>Start the server (python webui/server.py, it binds port 8000) in background, then POST to the endpoint and verify responses. NOTE: server may open a browser tab (webbrowser.open) — that's expected. Tests: (1) `{"count":3,"platform":"tiktok"}` → 200 JSON with 3 captions, each having text/platform/hook_type/cta/hashtags; (2) `{}` → 200 with defaults (10 captions tiktok); (3) `{"count":"abc"}` → 400; (4) `{"platform":"nope"}` → 400. Kill server after. Use Invoke-RestMethod or curl for the POSTs. If port 8000 busy, use an alternate port via PORT env or note the conflict.</action>
  <verify>$r = Invoke-RestMethod -Method Post -Uri http://localhost:8000/api/captions/generate -ContentType "application/json" -Body '{"count":3,"platform":"tiktok"}'; $r.captions.Count</verify>
  <acceptance_criteria>
    - Live POST returns ok:true with captions array
    - Each caption dict has the 5 contract keys
    - Invalid count → HTTP 400
    - Server process terminates after test
  </acceptance_criteria>
  <done>Live endpoint verified for happy path + 400s</done>
</task>

</tasks>
