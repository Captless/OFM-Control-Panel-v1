---
phase: 4
plan: 04-01
type: execute
wave: 1
depends_on: 03-01
files_modified:
  - .github/workflows/ci.yml
  - codemap.md
  - scripts/codemap.md
  - AGENTS.md
autonomous: true
requirements:
  - R1: Live server regression passes — all existing endpoints intact on :8000
must_haves:
  truths:
    - "CI includes scripts/alina_textgen.py syntax check"
    - "codemap.md + scripts/codemap.md document alina_textgen.py"
    - "AGENTS.md Active Components + Recent Changes document caption generator"
  artifacts:
    - ".github/workflows/ci.yml with scripts/alina_textgen.py in syntax-check"
    - "codemap.md scripts/ row updated (file count + entry)"
    - "scripts/codemap.md Files table + Usage updated"
    - "AGENTS.md Recent Changes entry (2026-08-06 caption generator)"
  key_links:
    - ".github/workflows/ci.yml syntax-check block"
    - "codemap.md scripts/ table row"
    - "scripts/codemap.md alina_textgen row"
---

<objective>
Integration & polish for the caption generator feature: verify no regressions in the existing photo pipeline / server, wire scripts/alina_textgen.py into CI, and update repo docs (codemap + AGENTS.md). Deliverables are docs/config — no application code changes.
</objective>

<execution_context>
Prior phases delivered: Phase 1 = scripts/alina_textgen.py (pool-based caption generator, CLI + batch_generate, seed, dedup); Phase 2 = POST /api/captions/generate in webui/server.py (count 1-20, platform in tiktok/reels/shorts/x/stories, hook_types list, seed int; ValueError→400, Exception→500); Phase 3 = Caption Generator UI card in webui/static/ (index.html .caption-gen-card, app.js generateCaptions/renderCaptions/copyCaption/copyAllCaptions/clearCaptions, style.css .caption-* classes). All phases committed on branch feature/ui-fix. Server currently running on :8000 (python webui/server.py) — server.py caches HOMEPAGE_HTML at module load, but static files served from disk (HTML/JS/CSS edits need no restart). CI = .github/workflows/ci.yml with 3 jobs (syntax-check py_compile explicit list, import-test, frontend-lint node --check). scripts/ has NO __init__.py (import via sys.path.insert in server). Shell is PowerShell on Windows; git-bash style blocks do NOT work — use PowerShell-native (; separators, Get-Content, etc.). GSD files written via gsd_gsd_write_state MCP tool with paths relative project root.
</execution_context>

<context>
This is the final phase. T4.1 must prove the three prior phases didn't break the existing pipeline (photo generation, prompt building, balances, outputs). T4.2 checks CI path coverage — scripts/alina_textgen.py is currently absent from the py_compile list, so CI won't catch syntax errors in it. T4.3 updates the docs that agents auto-load (codemap.md atlas + AGENTS.md) so the feature is discoverable. No functional changes; keep every edit minimal and consistent with existing file style. Do NOT touch STATE.md or ROADMAP.md (orchestrator owns those). Do NOT restart the running server unless a change actually requires it (server.py untouched in this phase — no restart needed).
</context>

<tasks>
<Task id="01" title="Regression check on :8000">
  Verify all existing endpoints still work against the running server:
  - GET /api/ping → ok:true
  - GET /api/presets → JSON with presets list
  - GET /api/outputs → JSON with outputs
  - GET /api/balance → JSON with balance
  - POST /api/captions/generate {"count":3,"platform":"tiktok","seed":1} → ok:true, 3 captions (caption feature still wired)
  - GET / returns index.html containing caption-gen-card
  Use curl.exe (Windows has curl built in; do NOT use Invoke-RestMethod for raw HTTP checks). Record results in the SUMMARY. Any failure = regression → report back, do not proceed silently.
</Task>

<Task id="02" title="CI coverage for scripts/alina_textgen.py">
  Edit .github/workflows/ci.yml:
  - Add `python -m py_compile scripts/alina_textgen.py` to the syntax-check job's run block (keep alphabetical-ish placement consistent with existing scripts/*.py entries, e.g. near scripts/run_tiktok.py or scripts/save_meta.py).
  - Import-test job: scripts/ has no __init__.py so `import scripts.alina_textgen` will NOT resolve. Instead add a line to the import-test run block that mirrors runtime resolution: `python -c "import sys; sys.path.insert(0, 'scripts'); import alina_textgen; print('scripts OK')"` (append after the existing api OK line).
  No other workflow changes.
</Task>

<Task id="03" title="Update codemap docs">
  Root codemap.md (directory table):
  - scripts/ row: File Count "6 files" → "7 files"; keep Responsibility string, update only count.
  scripts/codemap.md:
  - Add `alina_textgen.py` row to the Files table: "Identity-locked caption generator CLI - N captions for platform (tiktok/reels/shorts/x/stories), seed, prints to stdout" (fit the existing terse style).
  - Add to Usage block: `py scripts/alina_textgen.py 10 tiktok --seed 42    # 10 TikTok captions (identity-locked)`.
  - Update "Depends on" only if factually wrong — do not over-edit.
  webui/codemap.md:
  - server.py row already covers REST API; optionally append caption endpoint mention to Responsibility. Only if trivial; skip if awkward.
</Task>

<Task id="04" title="Update AGENTS.md">
  - File Structure tree: add `├── alina_textgen.py` under scripts/ (keep tree aligned).
  - Active Components: add short "Caption Generator" bullet under Web UI section (endpoint POST /api/captions/generate + UI card). Keep existing style (#### heading + bullet list, "**verified** ✓" pattern).
  - Recent Changes: append dated entry `### 2026-08-06 — Caption generator feature (phases 1-4)` summarizing: scripts/alina_textgen.py identity-locked pool generator; POST /api/captions/generate; UI card with platform/hook pills + copy; CI + docs. Use the established "**refactored** ✓ / **verified** ✓" terse style.
</Task>

<Task id="05" title="Final verification">
  - `python -m py_compile scripts/alina_textgen.py webui/server.py` clean.
  - `node --check webui/static/app.js` clean.
  - Live round-trip: POST /api/captions/generate count=2 → ok:true 2 captions (already covered in T4.1; confirm once more after edits).
  - Write 04-01-SUMMARY.md (GSD format: plan id, tasks, commits, verification, deviations).
  Commit with conventional message `docs(04-01): integration polish CI + docs` — BUT code/config edit (ci.yml) must be its own commit first: `ci(04-01): add scripts/alina_textgen.py to syntax and import checks`, then `docs(04-01): update codemap and AGENTS.md`, then `docs(04-01): complete integration polish plan (SUMMARY.md)`. Docs commits after code/config. Do NOT commit .planning state files other than the SUMMARY.md.
</Task>
</tasks>
